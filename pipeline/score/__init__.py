"""L5-L6: turn player-games into a ranked shortlist.

    standardize         the model -- three blocks: performance, market, motive
    export_candidates   seven cuts, then rank the survivors
    residualize         L5c, BUILT AND REJECTED (see ARCHITECTURE.md); one column,
                        the role `tier`, is still consumed by standardize
"""
